# Use official Node.js LTS image
FROM node:18-slim

# Install Chrome dependencies
RUN apt-get update && apt-get install -y \
    wget \
    gnupg \
    ca-certificates \
    fonts-liberation \
    libappindicator3-1 \
    libasound2 \
    libatk-bridge2.0-0 \
    libatk1.0-0 \
    libcups2 \
    libdbus-1-3 \
    libgdk-pixbuf2.0-0 \
    libnspr4 \
    libnss3 \
    libx11-xcb1 \
    libxcomposite1 \
    libxdamage1 \
    libxrandr2 \
    xdg-utils \
    libgbm1 \
    libxss1 \
    --no-install-recommends \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --only=production

# Copy application files
COPY . .

# Create a non-root user to run the app
RUN groupadd -r clockbot && useradd -r -g clockbot -G audio,video clockbot \
    && chown -R clockbot:clockbot /app

# Switch to non-root user
USER clockbot

# Set environment variable for Puppeteer to skip download (use installed Chrome)
ENV PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=false

# Start the application
CMD ["node", "index.js"]
